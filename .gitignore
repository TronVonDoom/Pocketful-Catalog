__pycache__/
*.pyc
.venv/
venv/

# The editor writes through a temporary file and renames it over the real one, so a
# crash mid-save leaves the old overrides intact. The temporary is never worth committing.
catalog/overrides.json.tmp

# Built release assets. Reproducible from catalog/ by pack.py.
dist/
